/* global before, chai, describe, it, PrettyError */

var expect = chai.expect;

describe( 'prettyFormError jQuery plugin', function() {
  it( 'is defined', function() {
    expect( lol ).to.be( 'undefined' );
  });
});
